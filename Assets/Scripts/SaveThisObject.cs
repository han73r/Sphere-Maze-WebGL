using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SaveThisObject : MonoBehaviour
{
 private void Awake()
 {
    /*SerializeField] private static 
    if (Instance == null)
    {
        Instance = this;
        DontDestroyOnLoad(gameObject);
    }
 */
 }
}
